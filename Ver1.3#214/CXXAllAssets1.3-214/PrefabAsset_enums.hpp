enum EPrefabVariantRuleType {
    SkipProp = 0,
    EPrefabVariantRuleType_MAX = 1,
};

enum class EPTUITheme {
    Default = 0,
    Dark = 1,
    EPTUITheme_MAX = 2,
};

enum class EPrefabVisualizerType {
    WireBox = 0,
    WireSphere = 1,
    EPrefabVisualizerType_MAX = 2,
};

